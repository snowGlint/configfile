# git configuration
git config --global  user.name  "xxx"
git config --global user.email  "706990@gmail.com"
git config --list

# start a local repository connected to github

echo "# rp-sftw" >> README.md
...
git init # 将目录初始化git目录

git add -A # 添加所有文件到暂存区
git commit -a -m "first commit" # 提交所有文件（-a），并写提交日志（-m)

git branch -M main # 对该git 目录建立一个main分支

git remote add origin https://github.com/sonwGlint/rp-sftw.git # 将该目录与远程github资源链接

git push -u origin main # 将本地目录资源推送到远程的github目录

