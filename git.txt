# git configuration
git config --global  user.name  "xxx"
git config --global user.email  "706990@gmail.com"
git config --list

# start a local repository connected to github

echo "# rp-sftw" >> README.md
...
git init

git add -A
git commit -a -m "first commit"

git branch -M main

git remote add origin https://github.com/sonwGlint/rp-sftw.git

git push -u origin main

